{
  "name": "alura-geek",
  "version": "1.0.0",
  "description": "Un SPA que permite el CRUD de productos del lado del cliente (FRONT) interactuando con una fuente de datos (BACK) y aplicando conceptos de asincronia, solicitudes HTTP, validacion de formularios y demas.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Emiliano G. Ferryra",
  "license": "ISC",
  "dependencies": {
    "express": "^4.21.1",
    "json-server": "^1.0.0-beta.3"
  }
}
